<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.BoardDAO">
	
	<insert id="dailyboard_Insert" parameterType="BoardVO">
	    insert into DAILY_BOARD(user_no, no, title, content, start_date, last_date, check_run, regdate)
        values(#{user_no}, board_no.nextval, #{title}, #{content}, #{start_date}, #{last_date}, '0', sysdate)
    </insert>
			
	    
    <select id="boardList" resultType="BoardVO">
	    select * from daily_board order by no desc
    </select>
    
    <select id="dailyboard_UpdateForm"  resultType="BoardVO" parameterType="BoardVO">
	    select * from daily_board where no=#{no}
    </select>
    
    <update id="dailyboard_Update" parameterType="BoardVO">
	    update daily_board 
		set title=#{title}, content=#{content}, start_date=#{start_date}, last_date=#{last_date}, regdate=sysdate
		where no=#{no}
    </update>
    
    <delete id="dailyboard_Delete" parameterType="BoardVO">
	    delete from DAILY_BOARD where no=#{no}
    </delete>
</mapper>  